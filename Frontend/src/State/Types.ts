export interface User{
    id? : string,
    username? : string,
    firstname? : string,
    latname?: string,
    email? : string,
    password? : string,
    avatar? : string,
    bio? : string,
    createdAt? : Date,
    updatedAt? : Date
}